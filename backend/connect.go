package main

import (
	"fmt"
	//"github.com/go-redis/redis/v8"
)

// client := redis.NewClient(&redis.Options{
// 	Addr:	  "localhost:6379",
// 	Password: "", // no password set
// 	DB:		  0,  // use default DB
// })

// func addToDictionary(s string) {
// 	var key = getKey()
// 	err := client.Set(key, s, 0)
// 	fmt.Println(s + "has been added to the server.")
// }
func diijd() {
	fmt.Println(" ")
}
